enum sprite_lib{
	idle,
	attacking,
	damaged
};

enum update_quest{
	activate,
	update,
	complete
};


enum quest_col{
		quest_id,
		quest_name,
		quest_state,
		quest_desc,
		obj_flag,
		obj_name
	}